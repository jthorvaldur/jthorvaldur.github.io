# Static review build

This GitHub Pages build contains precomputed outputs from the Python reference model. Scenario switching and transaction stepping work; arbitrary edits, persistence, and POST simulations require the live FastAPI deployment.
